"""Installer modules for JD2017 PC map installation.

Submodules:
    binary_generators: .act.ckd and .mpd.ckd binary file generators
    game_writer: PC MainScene generator (replaces AugustoDoidin's script)
    ipk_packer: UbiArt IPK archive pack/unpack
    secure_fat_maker: secure_fat.gf index generator (replaces Wukko's script)
    sku_scene: SkuScene ISC patcher for map registration
    texture_encoder: Texture conversion and CKD wrapping
"""
